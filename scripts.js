/* Add your JavaScript to this file */
/*window.onload = function() {
    const btn1 = document.getElementsByClassName("btn");
    const msg = document.getElementsByClassName("message");
    function outp(){
        msg.innerText = msg;
    }
    function validate(){
        var email = document.getElementById("email").value;

        var regExp = /^([a-zA-Z0-9\._]+)@([a-zA-Z0-9])+.([a-z]+)(.[a-z]+)?$/;

        btn1.addEventListener('click', outp);
        if (validate()){
            
            return true
        }
        else{
            alert("Please enter a valid email address.");
            return false
        }
    }

}*/